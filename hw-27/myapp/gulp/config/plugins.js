import replace from "gulp-replace"; // search and replacement
import browsersync from "browser-sync"; //instead of live-server

export const plugins = {
   replace: replace,
   browsersync: browsersync,
}